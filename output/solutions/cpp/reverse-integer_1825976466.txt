class Solution {
public:
    int reverse(int x) {
        string n = to_string(abs(x));

        std::reverse(n.begin() , n.end());

        int ans = stoi(n);
        if (x < 0) {
            ans *= -1;
        }

        return ans;
    }
};