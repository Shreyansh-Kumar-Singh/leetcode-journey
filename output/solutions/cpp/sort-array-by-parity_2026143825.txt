class Solution {
public:
    vector<int> sortArrayByParity(vector<int>& nums) {
        vector<int> ans;
        for (auto i : nums) {
            if (i % 2 == 0)
                ans.insert(ans.begin(), i);
            else
                ans.push_back(i);
        }
        return ans;
    }
};